version: '3'

services:

  backend:
    build: ./back
    container_name: moodpick-back
    ports:
      - "8000:8000"

  frontend:
    build: ./front
    container_name: moodpick-front
    ports:
      - "8501:8501"

    depends_on:
      - backend